# RAPPORT CRO — 2026-04-30 06:00 UTC

## Contexte snapshot
- Worker `trading-worker.service` actif depuis 2026-04-28 17:05 UTC (1j 12h, 206M RAM, 6m30 CPU)
- Equity LIVE futures : **$11,357** (snapshot 14:00 UTC 29-04)
- Equity Binance : **$10,594** (V10 snapshot DD 0.5%)
- Equity Alpaca paper : **$100,102**
- Positions LIVE : **0** (futures_positions_live.json = `{}`)
- Kill switch IBKR : **inactive armed** (reset 28-04 16:44 post-fix `85dbf39`)
- Kill switch crypto : **inactive**
- Job VPS `at #1` (cam_check 48h) en queue, scheduled 2026-05-01 16:55 UTC

## SCORE RISQUE : **8.5/10** (prod-ready avec reserves mineures)

Pas de regression vs audit 19 avr. Hardening propre depuis (commits `85dbf39`, `e6cb61e`).
Capital reel a risque a date = $0 (zero position live). Le risk-immediat-actuel est nul.

---

## CRITIQUES (fix immediat)
**Aucune.**

Verifications effectuees :
- ✅ `pre_order_guard` branche sur les 3 brokers avec fail-closed (Alpaca, IBKR, Binance) — `core/alpaca_client/client.py:445`, `core/broker/ibkr_adapter.py:330`, `core/broker/binance_broker.py:452`
- ✅ Boot defaut `PAPER_TRADING=true` (fail-safe explicite dans worker.py)
- ✅ Kill switch live armed, 5 thresholds configures (daily/hourly/5d/monthly/per-strat)
- ✅ Bracket watchdog tourne toutes les 6 min (verifie 0 position = nothing to check, propre)
- ✅ `.gitignore` blinde sur `.env`, `*.key`, `*.pem`, `*credentials*` ; aucun secret track via `git ls-files`
- ✅ Engine BacktesterV2 modifie n'est PAS importe par `core/runtime/` ni `core/worker/` — backtest only, zero impact live

---

## HAUTES (fix cette semaine)

### H1 — 258 lignes uncommitted dans `core/backtester_v2/engine.py`
**Constat** : ajout de `_ProtectiveExitState` (SL/TP/trailing intrabar) + test associe `tests/test_backtester_v2_trailing_stop.py`. Tests **3/3 passent**, semantique correcte (pessimiste : `fill_price = min(stop_price, bar.open)` pour stop long, watermark update apres check stop).

**Pourquoi HAUTE** : tout backtest lance maintenant avec cette branche locale n'est **pas reproductible** (working tree non versionne). Si Marc valide une nouvelle sleeve sur ce code, le commit de validation referencera le hash `e6cb61e` mais le code reel sera different. Risque de derive backtest/live silencieuse.

**Fix** : `git add core/backtester_v2/ tests/test_backtester_v2_trailing_stop.py && git commit -m "feat(backtester): protective exits intrabar (SL/TP/trailing)"` avant tout WF officiel.

### H2 — Pas detecte sur cette passe.

---

## MOYENNES (fix ce mois)

### M1 — `config/limits_live.yaml` obsolete
**Constat** : `last_update: 2026-03-27`, `capital: 10000`, secteurs equity (tech/finance/energy) alors que le live est 100% futures depuis fin mars. Reference par 5 fichiers core (`futures_runner.py`, `risk_manager_live.py`, `risk_budget_allocator.py`, `eu_fx_risk_calibrator.py`, `audit_trail.py`).

**Risque concret** : si une lecture de `max_position_pct: 0.20` ou `daily_loss_pct: 0.05` est utilisee comme guard, on travaille sur un capital fantome de $10K alors que NAV reel = $11,357. Pas une perte directe, mais decalage sizing potentiellement detectable a $50/position.

**Fix** : verifier qui lit reellement quoi (les 5 fichiers), aligner `capital` sur NAV effectif via lecture broker, ou marquer le fichier comme `_legacy` si superceded par `risk_budget_allocator`.

### M2 — Trou potentiel executeur CAM live (deja sous monitoring)
**Constat** : XMOMENTUM produit signaux 14:15 UTC (SPY/GLD/EURUSD weights), mais aucun routage observe vers IBKR live U25023333. Hypothese checkup 29-04 : cycle CAM live executor manquant ou XMOM pas branche.

**Sous monitoring** : job at #1 + cron session `59e8fac9` produiront le verdict 1er mai 19:03 Paris. Pas d'action a prendre maintenant.

### M3 — Reconciliation Alpaca paper divergente (signale 29-04)
**Constat** : broker Alpaca = 4 pos (AMAT/AMD/MU/TLT), simu locale = 11 pos (APA/CF/CNC/...), 0% overlap. Severity warning car `paper_only`, pas de push, mais decalage 100% suspect.

**Fix** : decider — soit on aligne (push simu vers broker + clean broker), soit on marque le book comme "broker source-of-truth" et on reset la simu locale.

---

## BASSES (ameliorations)

### B1 — Identite git non configuree sur VPS
- `git config user.email` retourne vide cote Hetzner
- Push HTTPS impossible (`could not read Username for github.com`)
- Bloque l'auto-commit cote VPS pour rapports automatises (cf chantier cam_check 48h)
- Fix : configurer un PAT GitHub + `git config user.email` si besoin futur de push depuis VPS

### B2 — Multiples brackets paper sur memes symboles (DUP573894)
- Vu dans checkup 29-04 : MES a 2 brackets OCA distincts, MNQ idem, MCL idem
- Comportement attendu (1 bracket / lot OCA) mais cree de la verbosite ; verifier que les OCA groups annulent bien proprement quand un lot ferme

### B3 — `last_updated` data tracking
- 23 incidents sur 2026-04-30 et 171 sur 2026-04-29 = volume de reconciliations Alpaca dominant. Filtrer ou ignorer pour reduire le bruit.

---

## VERDICT : **APPROUVE AVEC RESERVES**

**Scope autorise** : tout le perimetre operationnel actuel (paper futures, paper Alpaca, crypto idle, futures live a $11.3K).

**Reserves explicites** :
1. Commit obligatoire de `core/backtester_v2/engine.py` avant tout backtest officiel destine a une promotion paper→live (sinon non-reproductible).
2. Pas de promotion live d'une nouvelle sleeve tant que le verdict CAM live (1er mai) n'est pas connu — risque de pousser dans un desk dont l'executeur a un trou.
3. Aligner `config/limits_live.yaml` sur le NAV reel ou retirer le fichier si supercede par `risk_budget_allocator`.

**Aucun arret operationnel requis.** Kill switches armes, 0 position live, brackets en place sur paper, hardening recent valide.

---

Rapport genere par /cro — sources : VPS Hetzner (worker.log, state files, atq), git log/diff local, grep code base.
