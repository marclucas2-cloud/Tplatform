=== RAPPORT CRO — 2026-05-01 ===
Equity: $26,320.44 live IBKR | Positions: 1 live (`MCLM6`) | DD jour: ~0.0%
SCORE RISQUE: 7.3/10

CRITIQUES (fix immediat)
  - [x] Aucun critique ouvert au moment de la cloture de l'audit.
  - [x] Point critique potentiel verifie en premier: la position live `MCLM6` a bien un stop broker-side (`102.62`) et un take-profit broker-side (`108.90`) sur OCA `OCA_MCL_8c142d90`.

HAUTES (fix cette semaine)
  - [ ] `Cross-Asset Mom` live reste partiellement inexecutable sur les signaux `MNQ` au budget actuel: le 2026-04-30 14:00/14:14 UTC, `new risk $1629 > budget $1318`. Ce n'est pas un bug d'execution, mais un vrai ecart backtest/live sur l'univers executable.
  - [ ] La connectivite `IBKR 4002` a disparu environ 1h le 2026-05-01 entre ~04:28 UTC et ~05:29 UTC. Le risque etait borne par le SL broker-side, donc pas de perte non-bornee, mais le watchdog et la reconciliation etaient aveugles pendant la coupure.

MOYENNES (fix ce mois)
  - [ ] Le book `IBKR paper` `DUP573894` etait non canonique (positions nettes et ordres residuels incoherents, reliquats `MCLZ6`). Il a ete nettoye manuellement pendant l'audit, mais il manque encore un mecanisme automatique de reset/reconciliation paper pour eviter la recurrence.
  - [ ] La re-entry live `MCL` du 2026-04-30 est un override discretionnaire synchronise dans le state, pas un cycle normal du worker. C'est acceptable ponctuellement, mais il faut garder une doctrine claire pour les re-entrees manuelles.
  - [ ] `CLAUDE.md` reste narrativement stale sur plusieurs points d'etat desk (header as-of 2026-04-19), meme si le runtime audit canonique est bon.

BASSES (ameliorations)
  - [ ] Consolider la doc du risk budget futures pour expliciter que `CAM` peut rester dormant sur certains symboles tant que le book ne depasse pas certains paliers de NAV.
  - [ ] Ajouter un rappel d'audit matinal `IBKR 4002` pour verifier les flap de connectivite avant le cycle futures live.

VERDICT: AVEC RESERVES

Points verifies pendant l'audit:
  - `runtime_audit --strict` VPS: OK
  - `kill_switch_state.json`: `active=false`, `armed=true`
  - `crypto_kill_switch_state.json`: `active=false`
  - Position live `U25023333`:
      * `MCLM6 +1`
      * avg cost `10471.77` (= 104.7177)
      * market price ~`105.23`
      * unrealized PnL `+$51.23`
      * `STP 102.62` + `LMT 108.90` visibles broker-side
  - `live_risk_dd_state.json`: baseline rebasee a `26353.84`
  - `LIVE RISK SNAPSHOT` recent: equity stable autour de `$26.3K`, daily PnL autour de `0%`
  - Book paper `DUP573894`: remis a zero pendant l'audit (`positions=[]`, `orders=[]`, state file `{}`)

Lecture CRO:
  - Le risque live n'est pas non borne: stop broker-side, TP broker-side, kill switch arme.
  - Le plus gros risque residuel n'est plus un bug execution pur; c'est la combinaison:
      * budget live encore limite pour certains signaux `CAM`
      * connectivite `4002` fragile au petit matin
      * hygiene paper qui, sans nettoyage explicite, peut de nouveau polluer la lecture desk.

Actions recommandees:
  1. Garder `MCLM6` sous suivi normal; aucun fix runtime urgent requis tant que bracket present.
  2. Re-evaluer l'univers executable de `Cross-Asset Mom` si `MNQ` doit rester live_core sous ce niveau de NAV.
  3. Programmer un garde-fou de reconciliation/reset paper futures pour eviter un nouveau drift `DUP573894`.
  4. Mettre `CLAUDE.md` a jour au prochain passage doc pour reduire le drift narratif.

=========================================
