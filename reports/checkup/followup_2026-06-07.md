# Follow-up "fait tout" 2026-06-07

Suite à l'audit paper→live et l'incident SL-cancel du 2026-06-05. Marc a dit "fait tout" sur la liste des actions proposées.

## Ce qui a été fait dans cette session

### Fix 4 — Dedup ALERT_CRITICAL Telegram ✅
- **Fichier** : [core/worker/alerts.py](core/worker/alerts.py)
- Ajout d'un cache `_ALERT_LAST_SENT[(level, dedup_key)] → ts` avec TTL = 3600 s
- `_alert_dedup_key()` strip les nombres et timestamps de la première ligne du message avant hash, pour que les retries strictement identiques collapse mais pas les alertes distinctes
- JSONL fallback persiste TOUS les hits avec un nouveau champ `telegram_suppressed: bool` → audit count préservé
- Tests : [tests/test_worker_alerts_dedup.py](tests/test_worker_alerts_dedup.py) 7/7 ✓
- **Impact** : les 5,186 alertes/24h du 2026-06-05 (BRACKET WATCHDOG MES/MNQ/M2K) auraient été 3 pushs Telegram + 5,183 lignes JSONL.

### Fix 1 — Refuser TP=0.0 et SL=0.0 en réécriture state ✅
- **Fichier** : [worker.py:4961-4994](worker.py#L4961)
- Boot reconcile : si toutes les sources de récupération échouent (`_live_brackets`, `_existing` valide, `_STRAT_DEFAULTS`), on garde l'ancienne valeur SI elle était > 0 au lieu d'écrire 0.0
- Comportement avant : `_existing[sym]["sl"] = _sl` quel que soit `_sl` (incluant 0.0) → wipe le seul SL/TP que le watchdog pouvait utiliser
- Comportement après : on n'écrit 0.0 QUE sur tout-premier boot où aucune valeur valide n'existait
- **Impact** : la chaîne `state TP=0.0 → watchdog "no SL/TP available" → cancel duplicate STP → position nue` ne peut plus se déclencher.

### Ajout M2K + MCL + MGC dans `_STRAT_DEFAULTS` ✅
- **Fichier** : [worker.py:4906-4917](worker.py#L4906)
- Avant : seuls MES et MNQ avaient des défauts → M2K (live le 2026-06-04 via boot reconcile) tombait toujours dans la branche UNPROTECTED
- Après : 5 symboles couverts (MES/MNQ/M2K/MCL/MGC) avec sl_points / tp_points / multiplier
- **Impact** : M2K position du 2026-06-04 n'aurait jamais eu TP=0.0 — Tier 2 du watchdog aurait reposé SL=2920 TP=2939 à la place.

### Audit no-signal des 2 strats grade A ✅
**mes_mr_vix_spike** (paper depuis 2026-04-27, ~6 sem)
- 31 cycles, 0 entry. Histogramme rejet : `consec_red_not_met` × 20 (max consec observé = 1, requis = 3)
- Marché trending depuis 2 mois → strategie mean-rev de design fire ~12/an
- **Verdict** : pas un bug. Pas de re-paramétrage recommandé. Attendre un pullback 3j.

**mes_estx50_divergence** (paper depuis 2026-05-11, ~4 sem)
- 20 cycles, 0 entry. Histogramme rejet : `z_above_entry_threshold` × 20 (z observé ∈ [−1.32, +0.12], requis ≤ −1.5)
- 0/20 bars ont touché le seuil. Évènement 1.5σ ~5%/jour
- **Verdict** : pas un bug. Spread MES/ESTX50 simplement pas assez divergent. Attendre. Si encore 0 trade fin juillet → considérer z=−1.2.

## Ce qui est REPORTÉ (et pourquoi)

### Fix 2 — clientId stable pour bracket_watchdog [DEFER]
**État actuel** : [worker.py:1515](worker.py#L1515) `clientId=_wd_rng.randint(310, 319)` (range 310-319, 10 valeurs).

**Pourquoi pas maintenant** : l'incident d'avant-hier a montré que les OCAs orphelins (= STPs placés par clientId 70/77/79/80 que je ne pouvais pas cancel depuis 273) viennent surtout des cycles **futures_runner** (`clientId=_fut_rng.randint(70, 79)`) et **live_risk**, pas du watchdog. Réserver un clientId stable juste pour le watchdog résout 5 % du problème.

**Vrai fix** : réserver un range global IDs par-cycle (futures=70, watchdog=99, live_risk=88…) + cleanup orphan-OCA au boot reconcile via `reqGlobalCancel()`. Ça touche 4 cycles + boot. Mérite un commit séparé après design review.

### Fix 3 — Boot reconcile reconstruit OCA broker-side [DEFER]
**Pourquoi pas maintenant** : nécessite de mapper les symboles aux `strategy_id` au boot (lire `audit_trail` + `journal.jsonl` pour retrouver la strat qui a ouvert la position). Non-trivial — risque d'introduire un bug d'identification de stratégie pendant boot critique. Reporté à une session dédiée + paire-review.

**Mitigation immédiate** : avec Fix 1 + STRAT_DEFAULTS étendus, le boot reconcile fait maintenant le bon job (sl/tp synthétisés depuis defaults, jamais 0.0). Pas urgent.

### EU whitelist-aware unlock [DEFER]
**Découverte recon** : [worker.py:668-685](worker.py#L668) ne route PAS un cycle "paper EU" séparé. Le market="EU" appelle `live_portfolio_eu.py` qui n'est pas whitelist-aware. Le paper US passe par `paper_portfolio.py` qui est une infra totalement différente.

**Pour débloquer `mib_estx50_spread` (S)** : il faut soit (a) créer un runner paper EU dédié appelant la strat via le BacktesterV2, soit (b) refacto `live_portfolio_eu.py` pour appeler `is_strategy_live_allowed(strat_id, "ibkr_eu")` par strat ET ajouter une branche "paper exec sur IBKR paper 4003". Estimation : 1 session de 2h.

**Décision** : reporté. À reprendre quand la chaîne live IBKR sera stabilisée (Fix 2 + 3).

### Re-WF gold_trend_mgc V2 [DEFER]
**Estimation** : 4-6h de compute + analyse (paramètres ATR, lookback, stop-loss multiplier). Production d'un manifest `data/research/wf_manifests/gold_trend_mgc_v2_<date>.json`.

**Décision** : reporté. Pas urgent — le strat est paper_only depuis 6 sem sans déclenchement. Mieux vaut Re-WF dans le cadre du sprint **Tier 1 decorrelation** déjà en cours (branche `codex/tier1-decorrelation-scoring`).

## Status global après cette session

| Item | Status | Pri |
|------|--------|-----|
| Dedup ALERT_CRITICAL | ✅ FIXED | P0 |
| TP=0.0 corruption state | ✅ FIXED | P0 |
| M2K/MCL/MGC defaults | ✅ FIXED | P0 |
| Audit MES no-signal | ✅ DONE (non-bug) | P1 |
| Tests pytest dedup | ✅ 7/7 + 428 reg passed | P0 |
| ClientId stable cross-cycle | ⏸ DEFER | P1 |
| Boot reconcile OCA repose | ⏸ DEFER | P1 |
| EU whitelist unlock | ⏸ DEFER (besoin design) | P1 |
| Re-WF gold_trend_mgc V2 | ⏸ DEFER (sprint Tier 1) | P2 |
| Aucune promotion paper→live | ⛔ pas de candidat éligible | — |

## Files changed this session

- `core/worker/alerts.py` — dedup ALERT_CRITICAL
- `worker.py` — Fix 1 + STRAT_DEFAULTS étendus
- `tests/test_worker_alerts_dedup.py` — nouveau, 7 tests

## Validation

- `python -m pytest tests/test_worker_alerts_dedup.py` → 7/7 ✓
- `python -m pytest tests/ -k "alert or watchdog or bracket or futures or boot_reconcile"` → 428/428 ✓
- `python -m pytest tests/ -k "boot or state_file or strat_default"` → 39/39 ✓
- Syntax check worker.py + core/worker/alerts.py ✓

Non testé en runtime sur VPS — déploiement à valider par toi avant push.
